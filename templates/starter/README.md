# {{PROJECT_TITLE}}

Generated by [fc-admin](https://github.com/skyuming/fc-admin) on {{GENERATED_AT}}.

A Vue 3 + Vite + TypeScript + Pinia + Element Plus admin starter, modeled after `vue3-element-admin`.

## Quick start

```bash
# Install dependencies
pnpm install    # or npm install / yarn

# Run dev server
pnpm dev

# Build for production
pnpm build:prod
```

Default dev URL: http://localhost:{{DEV_PORT}}

## Built-in test account (Mock mode only)

When `VITE_USE_MOCK=true` (default in `.env.development`), login uses an in-memory mock backend (no real server needed).

| Field | Value |
|---|---|
| **Username** | `admin` |
| **Password** | `admin123` |
| **Role** | `ROOT` (bypasses all `v-has-perm` checks) |
| **Permissions** | All `sys:user:* / sys:role:* / sys:menu:* / sys:dept:* / sys:dict:* / sys:region:*` |

**To switch to a real backend:**

1. Edit `.env.development`: set `VITE_USE_MOCK = 'false'`
2. Set `VITE_APP_BASE_API` to your backend URL
3. Ensure your backend exposes the same endpoints (see `src/mock/handlers/auth.ts` and `src/mock/handlers/system.ts` for the expected shapes)
4. Delete `src/mock/` directory when going to production

⚠️ **Never deploy with `VITE_USE_MOCK=true` to production.**

## What's included

- **Auth flow**: login page, token storage, permission tree, route guards.
- **Layout shell**: header, sidebar, breadcrumbs, tags-view, fullscreen, language switch, theme toggle.
- **Core admin pages** under `src/views/system/`:
  - User management
  - Role management
  - Menu management
  - Department management
  - Dictionary management
  - Region management
  - Template management
- **API infrastructure** under `src/api/`:
  - `auth/` (login / logout / userinfo)
  - `user/`, `role/`, `menu/`, `dept/`, `dict/`, `file/`
  - Global `adapter.ts` + `adapter.web.ts` (request layer)
- **Directives**: `v-has-perm` (button-level permission), `loadmore`.
- **Pinia stores**: `user`, `permission`, `app`, `settings`, `tagsView`.
- **Mock data** under `src/mock/data/` (for development without a backend).

## What's NOT included

This is a **starter**, not a copy of `future-community-manage`. It intentionally omits business modules such as `cockpit`, `community`, `integral`, `gongchenqiao`, `taizhoulu`, `jianlong`, `xixing`, `wenzhou`, `qjyCommunity`, etc.

Add your own business modules with [fc-admin-crud](https://github.com/skyuming/fc-admin#skills):

```
fc-admin, 帮我加一个 XX 模块
```

## Project layout

```
src/
├── api/                       # HTTP API modules
│   ├── adapter.ts             # global request adapter
│   ├── adapter.web.ts
│   ├── auth/                  # login / logout
│   ├── user/, role/, menu/, dept/, dict/, file/
│   └── types/
├── assets/                    # images, fonts, icons
├── components/                # shared components (Breadcrumb, SvgIcon, ...)
├── config/
├── directive/                 # v-has-perm, v-loadmore
├── enums/
├── lang/                      # i18n
├── layout/                    # app shell
├── mock/                      # dev-only mock data
├── router/                    # vue-router
│   └── index.ts               # constant + dynamic routes
├── store/                     # pinia
│   ├── index.ts
│   └── modules/               # user, permission, app, settings, tagsView
├── styles/
├── types/                     # TypeScript type definitions
├── utils/                     # request, auth, helpers
└── views/                     # page components
    ├── login/
    ├── dashboard/
    ├── system/                # core admin pages (user, role, menu, dept, dict, region, template)
    ├── error-page/
    ├── noPermission/
    └── redirect/
```

## Configuration

- `.env.development` — local dev
- `.env.staging` — pre-prod build
- `.env.production` — production build

See `fc-admin-arch/references/env-config.md` for the full variable list.

## License

This starter is generated from a project originally licensed under MIT.
